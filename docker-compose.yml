name: percepta-inferencer

networks:
  percepta:
    external: true

services:
  percepta-inferencer:
    image: percepta-inferencer:latest

    container_name: inferencer

    ports:
      - "8001:8001"

    volumes:
      - ./src:/app/src
    
    networks: 
      - percepta
